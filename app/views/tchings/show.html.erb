
<div class="row">
  <div class="col-lg-8 offset-lg-2 col-md-8 offset-md-2 col-sm-6">
        <div class="text-center">
         <h3><%= @tching.title %></h3>
        </div>
     <!--<div class="form-group" > 
        <textarea class="form-control" rows="15" readonly><%=@tching.content %></textarea>
      </div> -->
<div class="table-responsive">
    <table class="table table-striped table-bordered ">
       <% urchgtext = text_url_to_link(simple_format(h(@tching.content))).html_safe %>
      <td><%= urchgtext %></td>
  
    </table>
</div>

<% if current_user.id ==1 %>

    <div class="center-block">
        <div class="btn-group">
            <%= link_to '新規投稿', new_tching_path(@ctg), class: 'btn btn-primary btn-sm' %>
        </div>
        <div class="btn-group">
            <%= link_to '投稿を編集する', edit_tching_path(@tching) ,class: 'btn btn-primary btn-sm'%>
         </div>  
         <div class="btn-group">
            <%= link_to '投稿を削除する', @tching, method: :delete, data: { confirm: '本当に削除してよろしいですか？' },class: 'btn btn-danger btn-sm' %>
          </div>  
    　</div>
    
<% end %>
　　
　　
    <div class="text-center">　
       
        <%= link_to 'タイトル一覧に戻る', tchings_path(ctg_id: @ctg_id) %>
    　　<%= link_to 'カテゴリー一覧に戻る', ctgs_path, class: 'baktop' %>
    </div>
 
 </div>
</div>
